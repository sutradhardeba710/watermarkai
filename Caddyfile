{
	# Global options
	# admin off
}

:80 {
	# Route /api and /health to the backend
	handle_path /api/* {
		reverse_proxy backend:8000
	}
	
	handle_path /health/* {
		reverse_proxy backend:8000
	}

	# Route everything else to the frontend
	handle {
		reverse_proxy frontend:3000
	}
}
